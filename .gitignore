# dependencies
node_modules/
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# next.js
/.next/
/out/
next-env.d.ts

# production build
/build

# misc
.DS_Store
*.pem

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local env files (never commit secrets)
.env
.env*.local

# vercel local project metadata
.vercel/

# supabase
supabase/.temp/
supabase/.branches/

# testing
/coverage

# editors
.vscode/
.idea/
*.swp

# claude code (agent worktrees, local settings)
.claude/

# os
Thumbs.db

# typescript incremental build cache
*.tsbuildinfo

# Throwaway agent working files. Never part of a release — an earlier wave left
# merged i18n fragments and PGlite probe harnesses here, and a `git add -A` would
# have swept them into a commit.
scratchpad/
